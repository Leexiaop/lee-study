<!--index.wxml-->
<view class="detail">
	<block wx:if="{{isLoading}}">
		<view class="loading">
			<van-loading type="spinner" color="#fff" />
		</view>
	</block>
	<block wx:else>
		<towxml nodes="{{answer}}"/>
	</block>
</view>
